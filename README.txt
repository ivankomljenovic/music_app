Goal is to develop an app to aid with praciting a wind instrument

App features to include:
-Scale generator, ask the user to play a random scale 
-Add options for all scales, major, minor, pentatonic, different type of minor scales
-Should be comprehensive, so that the musician practices all scales
-Should have some form of corrective option. eg. did you get the scale right?
-designed for people who are learning their scales.

-Transposition trainer
-transpose from this key to that key
-transpose from this instrument to that instrument


-Simple progression tracker
-so the app knows how you are improving over time

-Simple interface, with certain types of excercises, how to do the exercise, etc.
-e.g. 'Begin Scale Training'
-then input how you did, did you make a mistake?

-first feature to make,
-First Scale exercise, all the major scales.
-randomly generate a major scale, have the user play the scale, ask the user how they did, get them to confirm if they did it correctly or not.

I think that the name of the app should be something to do with music fundamentals, like mastering essential performance skills for wind musician.



What setup to use for this project?
-I want to have this app run on my apple phone, so should use
React Native or Flutter. So that I can also deploy the app to Android I guess.

Android:
-Kotlin programming language and Android SDK
-apps are made in Android Studio

iOS:
-Swift programming language
-Xcode is the IDE

-React Native (framework by Meta for app dev both Android and iOS)
-JavaScript programming language
-VS Code IDE, or the native ones (Android Studio & Xcode)
-Expo framework built on top of this to simplify things


-Flutter (framework by Google for app development for both Android and iOS)
-Dart progrmaming language
-VS Code IDE, or the native ones


-Backend features, Firebase e.g. Has support for authentication services, notifications, etc.
-Frontend languages: HTML, CSS, JavaScript

-Material UI - React component library
-Bootstrap - popular CSS front-end framework


Angular, seems like this is more for web UI development
-front end UI development?
-uses TypeScript as the programming language (superset of JavaScript that uses static typing (ie. types determined at compile time instead of runtime))
-VS Code IDE

cross-platform frameworks: Ionic, Cordova